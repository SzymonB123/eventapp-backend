<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
          http://www.liquibase.org/xml/ns/dbchangelog
          http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="003-create-event-registrations-table" author="budzin">
        <createTable tableName="event_registrations">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="event_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="user_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="status" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="cancelled_at" type="TIMESTAMP"/>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="event_registrations"
                baseColumnNames="event_id"
                referencedTableName="events"
                referencedColumnNames="id"
                constraintName="fk_event_registrations_event"/>

        <addForeignKeyConstraint
                baseTableName="event_registrations"
                baseColumnNames="user_id"
                referencedTableName="users"
                referencedColumnNames="id"
                constraintName="fk_event_registrations_user"/>

        <addUniqueConstraint
                tableName="event_registrations"
                columnNames="event_id, user_id"
                constraintName="uk_event_registrations_event_user"/>

        <createIndex tableName="event_registrations" indexName="idx_event_registrations_event_id">
            <column name="event_id"/>
        </createIndex>

        <createIndex tableName="event_registrations" indexName="idx_event_registrations_user_id">
            <column name="user_id"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>